{
  "name": "discipline-hub-sync-server",
  "version": "1.0.0",
  "description": "Sync server for Discipline Hub app",
  "main": "sync-server.js",
  "scripts": {
    "start": "node sync-server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
